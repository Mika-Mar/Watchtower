services:
  watchtower:
    build: .
    container_name: watchtower
    restart: "no"

    environment:
      BARK_DEVICE_KEY: ${BARK_DEVICE_KEY:?Set BARK_DEVICE_KEY in .env}
      BARK_SERVER: ${BARK_SERVER:-https://api.day.app}

    volumes:
      - ./data:/app/data
      - ./config:/app/config
